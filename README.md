# SNBackupBundle

## Configuration

config.yml

```yaml
    sn_backup:
        backup_folder: "%kernel.root_dir/../var/backup"
        database:
            host: "%database_host%"
            dbname: "%database_name%"
            user: "%database_user%"
            password: "%database_password%"
```